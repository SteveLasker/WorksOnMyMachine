version: '2'

services:
  worksonmymachine:
    image:
      stevelasker/worksonmymachine:debug-latest
    environment:
      - REMOTE_DEBUGGING=${REMOTE_DEBUGGING}
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "80:80"
    volumes:
      - .:/app
